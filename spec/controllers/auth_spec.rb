require_relative '../spec_helper.rb'

describe "Authentication" do
  it "should authenticate user if token exists" # cookie, model in DB, current user and all
  it "should not authenticate user if token is missing"
end